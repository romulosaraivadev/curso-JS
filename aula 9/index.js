//string, number, undefined, null, boolean
const nome = 'romulo' //string
const nome1 = "romulo" //string
const nome2 = `romulo` //string
const num1 = 10 //number
const num2 = 10.52  //nubem
let numeAluno  //undefined -> não aponta para nenhum local na memória
let sobrenomeAluno = null //nulo -> não aponta para nenhum local na memória
const aprovado = false //boolean = true, false (lógico)

let a = 2
const b = a

console.log (a, b)

a = 3

console.log (a, b)